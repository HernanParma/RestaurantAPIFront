/* export function showToast(kind, text, target) {
  target.className = `alert alert-${kind}`;
  target.textContent = text;
  target.classList.remove('d-none');
  setTimeout(() => target.classList.add('d-none'), 3500);
}
 */